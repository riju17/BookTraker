# Copy this file to .streamlit/secrets.toml (git-ignored) and adjust for deployment.

# Absolute or relative path to the SQLite database file.
# BOOK_TRACKER_DB = "/mount/data/book_tracker.db"

# Whether to seed sample books/sessions/goals (set to "0" or "false" in production).
# SEED_SAMPLE_DATA = "0"
